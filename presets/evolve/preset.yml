name: Evolve Mode
description: A self-improving search agent. Everything creator mode can do, plus the tools to run a candidate search and rewrite its own search machinery while the run is in progress.
order: 5
